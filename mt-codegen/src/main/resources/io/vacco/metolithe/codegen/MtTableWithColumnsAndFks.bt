CREATE TABLE IF NOT EXISTS {{schemaPrefix}}{{table.name}} (
{{for i, col in table.columns}}
  {{col.name}} {{col.type}}{{if col.nullable == false}} NOT NULL{{end}}{{if col.primaryKey}} PRIMARY KEY{{end}}{{if (i != table.columns.size() - 1) || (table.fKeys.size() > 0)}},{{end}}
{{end}}
{{for i, fk in table.fKeys}}
  CONSTRAINT {{fk.name}} FOREIGN KEY ({{fk.fromCol}}) REFERENCES {{fk.to}}({{fk.toCol}}){{if (i != table.fKeys.size() -1)}},{{end}}
{{end}}
)